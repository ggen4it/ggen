<section class="container mx-auto px-4 mt-16">
    <div class="section-pattern featured-ggen">
        <div class="flex flex-col lg:flex-row lg:items-end lg:justify-between gap-6 mb-10">
            <div>
                <h2 class="section-title mb-4">{{ trans('ggen.featured.title') }}</h2>
                <p class="section-subtitle">{{ trans('ggen.featured.subtitle') }}</p>
            </div>
            <a href="/collections" class="ggen-btn ggen-btn--outline">{{ trans('ggen.featured.all_products') }}</a>
        </div>
        <div class="featured-ggen__list">
            <article class="ggen-card">
                <div class="featured-ggen__image">
                    <img src="{{ 'images/placeholder.png' | asset }}" loading="lazy" width="480" height="320" alt="{{ trans('ggen.featured.milker.image_alt') }}">
                </div>
                <span class="ggen-badge mb-4">{{ trans('ggen.featured.milker.badge') }}</span>
                <h3 class="text-2xl font-semibold mb-3 text-[var(--ggen-color-dark)]">{{ trans('ggen.featured.milker.title') }}</h3>
                <p class="text-gray-700 mb-6">{{ trans('ggen.featured.milker.description') }}</p>
                <a href="/product/green-gen-milker" class="ggen-btn">{{ trans('ggen.featured.cta') }}</a>
            </article>
            <article class="ggen-card">
                <div class="featured-ggen__image">
                    <img src="{{ 'images/placeholder.png' | asset }}" loading="lazy" width="480" height="320" alt="{{ trans('ggen.featured.zapper.image_alt') }}">
                </div>
                <span class="ggen-badge mb-4">{{ trans('ggen.featured.zapper.badge') }}</span>
                <h3 class="text-2xl font-semibold mb-3 text-[var(--ggen-color-dark)]">{{ trans('ggen.featured.zapper.title') }}</h3>
                <p class="text-gray-700 mb-6">{{ trans('ggen.featured.zapper.description') }}</p>
                <a href="/product/solar-bug-zapper" class="ggen-btn">{{ trans('ggen.featured.cta') }}</a>
            </article>
        </div>
    </div>
</section>
